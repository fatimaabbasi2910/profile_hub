# ProfileHub

A Flutter app that fetches and displays random users using API integration.

## Features
- Splash screen with animation
- Fetch random users from API
- Display user info with avatar, name, email
- Beautiful UI with gradient app bar

## How to Run
1. Clone the repository
2. Run `flutter pub get`
3. Run `flutter run`

## Author
Fatima Abbasi
